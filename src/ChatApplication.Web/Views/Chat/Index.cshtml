@using ChatApplication.Domain.Entities
@model IList<GroupChat>
@{
    ViewData["Title"] = "All Chats";
}

<h2>All Chats</h2>
<table class="table table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Group Name</th>
            <th>Created By ID</th>
            <th>Created At</th>
            <th>Enter Chat</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var chat in Model)
        {
                <tr>
                    <td>@chat.Id</td>
                    <td>@chat.GroupName</td>
                    <td>@chat.CreatedById</td>
                    <td>@chat.CreatedAt.ToString("yyyy-MM-dd HH:mm:ss")</td>
                    <td><button class="button-65" role="button" onclick="openChatWindow('@chat.Id', '@chat.GroupName')">Kirish</button></td>
                </tr>
        }
    </tbody>
</table>
<script>
    function openChatWindow(chatId, chatName) {
        window.location.href = '/Chat/ChatRoom?chatId=' + chatId + '&chatName=' + chatName;
    }
</script>